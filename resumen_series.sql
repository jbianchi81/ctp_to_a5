\copy (select estaciones.nombre,estacion_id,series.id,min(timestart),max(timestart) inicio,count(timestart) fin,min(valor),max(valor) from estaciones join series on estacion_id=unid left join observaciones on series_id=series.id left join valores_num on obs_id=observaciones.id where tabla='ctp' and var_id=1 and proc_id=6 and series.unit_id=22 group by estaciones.nombre, estacion_id , series.id order by estacion_id) to 'resumen_series_analisis.csv' with csv;
\copy (select estaciones.nombre,estacion_id,series.id,min(timestart),max(timestart) inicio,count(timestart) fin,min(valor),max(valor) from estaciones join series on estacion_id=unid left join observaciones on series_id=series.id left join valores_num on obs_id=observaciones.id where tabla='ctp' and var_id=1 and proc_id=3 and series.unit_id=22 group by estaciones.nombre, estacion_id , series.id order by estacion_id) to 'resumen_series_interpolada.csv' with csv;
